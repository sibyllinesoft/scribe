# @sibyllinesoft/scribe-linux-arm64

Platform-specific binary for Scribe. This package is automatically installed as a dependency of `@sibyllinesoft/scribe`.

Do not install this package directly. Instead, install the main package:

```bash
npm install @sibyllinesoft/scribe
```

